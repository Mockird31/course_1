#ifndef _ARRAY_H_
#define _ARRAY_H_
#include <stdio.h>

/**
 * @brief Prints the elements of an integer array.
 *
 * This function iterates over an array of integers and prints each element followed by a space.
 *
 * @param arr A pointer to the first element of the array to be printed.
 * @param n The number of elements in the array.
 */
void print_arr(int *arr, size_t n);

#endif
